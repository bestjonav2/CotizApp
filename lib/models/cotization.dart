import 'package:flutter/material.dart';

class Cotization {
  String id;
  String description;
  double averageCost;
  AssetImage image;

  Cotization(this.id, this.description, this.averageCost, this.image);
}